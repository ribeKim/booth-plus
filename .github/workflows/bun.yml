name: Bun workflow

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, closed]

permissions:
  contents: read

jobs:
  verify:
    if: github.event.action != 'closed'
    runs-on: ubuntu-22.04
    env:
      DATABASE_URL: postgresql://booth_plus:booth_plus@localhost:5432/booth_plus
      DATABASE_SSL_MODE: disable
    services:
      postgres:
        image: postgres:17.10-alpine3.24
        env:
          POSTGRES_DB: booth_plus
          POSTGRES_USER: booth_plus
          POSTGRES_PASSWORD: booth_plus
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U booth_plus -d booth_plus"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.1

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Apply PostgreSQL migrations
        run: bun run db:migrate

      - name: Check workspace
        run: bun run check

      - name: Test PostgreSQL integration
        run: bun run test:integration

      - name: Build workspace
        run: bun run build

      - name: Build backend container
        run: docker build -f apps/backend/Dockerfile -t booth-plus-backend:ci .

  release:
    if: github.event.action == 'closed' && github.event.pull_request.merged == true
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Read extension version
        id: package-version
        shell: bash
        run: |
          version="$(node -p 'require("./apps/extension/package.json").version')"
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Check release tag
        id: release-tag
        shell: bash
        run: |
          set +e
          git ls-remote --exit-code --tags origin "refs/tags/${{ steps.package-version.outputs.version }}" >/dev/null
          status=$?
          set -e

          case "$status" in
            0) echo "exists=true" >> "$GITHUB_OUTPUT" ;;
            2) echo "exists=false" >> "$GITHUB_OUTPUT" ;;
            *) exit "$status" ;;
          esac

      - name: Setup Bun
        if: steps.release-tag.outputs.exists == 'false'
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.1

      - name: Install dependencies
        if: steps.release-tag.outputs.exists == 'false'
        run: bun install --frozen-lockfile

      - name: Check workspace
        if: steps.release-tag.outputs.exists == 'false'
        run: bun run check

      - name: Build extension archive
        if: steps.release-tag.outputs.exists == 'false'
        run: bun run zip

      - name: Resolve extension archive
        if: steps.release-tag.outputs.exists == 'false'
        id: extension-archive
        shell: bash
        run: |
          shopt -s nullglob
          archives=(apps/extension/dist/*.zip)
          if [ "${#archives[@]}" -ne 1 ]; then
            echo "Expected exactly one extension archive, found ${#archives[@]}" >&2
            exit 1
          fi
          echo "path=${archives[0]}" >> "$GITHUB_OUTPUT"
          echo "name=$(basename "${archives[0]}")" >> "$GITHUB_OUTPUT"

      - name: Create release
        if: steps.release-tag.outputs.exists == 'false'
        id: create-release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.package-version.outputs.version }}
          release_name: v${{ steps.package-version.outputs.version }}
          body: ${{ github.event.pull_request.body }}
          draft: false
          prerelease: false

      - name: Upload release asset
        if: steps.release-tag.outputs.exists == 'false'
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create-release.outputs.upload_url }}
          asset_path: ${{ steps.extension-archive.outputs.path }}
          asset_name: ${{ steps.extension-archive.outputs.name }}
          asset_content_type: application/zip
